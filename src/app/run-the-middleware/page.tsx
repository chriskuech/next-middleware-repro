export default function RunTheMiddleware() {
  return (
    <div>
      <h1>The repro failed!</h1>
      <p>
        If the bug repros, then dev server should crash before this page can
        load.
      </p>
    </div>
  );
}
